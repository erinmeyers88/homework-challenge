export type MapCard = {
  id: string;
  label: string;
  queryType: string;
  imageUrl: string;
  description: string;
  subLabel: string;
  visible: boolean;
  dataUrl: string;
  configUrl: string;
};
